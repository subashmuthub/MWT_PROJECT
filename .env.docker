# Docker Production Environment Variables
# Copy this file to .env and update the values

# Database Configuration
DB_ROOT_PASSWORD=labms_root_secure_2024
DB_NAME=labms_db
DB_USER=labms_user
DB_PASSWORD=labms_password_secure_2024

# JWT Configuration - CHANGE THIS IN PRODUCTION
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-docker-2024

# Gmail Configuration for OTP (Optional)
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-gmail-app-password

# Google OAuth Configuration (Optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost/oauth/google/callback

# GitHub OAuth Configuration (Optional)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost/oauth/github/callback

# OpenAI Configuration for Chatbot (Optional)
OPENAI_API_KEY=your-openai-api-key

# Server Configuration
NODE_ENV=production
PORT=5000

# Application URLs
FRONTEND_URL=http://localhost
BACKEND_URL=http://localhost:5000